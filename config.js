/**
 * ♟️ Chess Platform - Point Configuration
 * Энэ файлд бүх point-ийн утгуудыг тохируулна уу
 */

module.exports = {
  // ── Бүртгэл & Нэвтрэлт ──────────────────────────────────────────
  REGISTER_BONUS:        10000,  // Шинээр бүртгүүлсэнд
  LOGIN_BONUS:           10000,  // Өдөр бүр нэвтэрсэнд (өдөрт нэг удаа)
  
  // ── Найз урих ────────────────────────────────────────────────────
  REFERRAL_INVITER:       5000,  // Найзаа урьсан хүнд
  REFERRAL_INVITED:       5000,  // Урилгаар бүртгүүлсэн хүнд

  // ── Хүлээх цаг ───────────────────────────────────────────────────
  WAITING_POINT_PER_MIN:   100,  // Lobby-д хүлээсэн 1 минут бүрт

  // ── Тоглоом ──────────────────────────────────────────────────────
  WIN_BONUS:              5000,  // Тоглоом хожсонд нэмэлт
  DRAW_BONUS:             1000,  // Тэнцсэнд
  LOSS_BONUS:                0,  // Хожигдсонд

  // ── Bet система ───────────────────────────────────────────────────
  BET_MIN:                1000,  // Хамгийн бага bet
  BET_MAX:             1000000,  // Хамгийн их bet
  BET_PLATFORM_FEE:       0.10,  // Платформын шимтгэл (10%)

  // ── Point шилжүүлэх ──────────────────────────────────────────────
  TRANSFER_MIN:           1000,  // Хамгийн бага шилжүүлэг
  TRANSFER_FEE:           0.02,  // Шилжүүлгийн шимтгэл (2%)

  // ── Эхлэлийн point ───────────────────────────────────────────────
  STARTING_POINTS:       10000,  // Шинэ тоглогчийн эхлэлийн point
};
